# 以不可变发布快照隔离同步与公开读取

外部资料先按刷新运行暂存到规范化工作区，再基于明确的当前快照编译成包含页面、地图和搜索文档的不可变发布快照；发布通过 compare-and-swap 确认当前指针仍等于该基线，最后才在短事务中原子切换，公开请求从不读取同步中的数据。相比直接从规范表实时拼装页面，这个选择增加了投影存储和重新编译成本，却能防止并发刷新覆盖较新资料，保证一次响应自洽、同步失败时继续使用最后成功的数据组、缓存安全以及快速回退，并避免为此引入事件总线或微服务。
