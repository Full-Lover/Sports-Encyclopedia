# 采用模块化单体架构

项目使用一个代码库、一个版本、一个可部署应用和一个事务型数据存储；Web、每日资料刷新与数据库迁移是同一应用的不同运行入口，业务 Module 只通过进程内 Interface 协作。相比微服务，这个选择更符合单人爱好项目、有限数据规模和低运维预算，同时仍以 `AtlasRegistry`、`AtlasRefresh`、`PublishedAtlas` 与 `WebExperience` 的数据所有权和依赖规则防止单体退化为大泥球；具体框架、数据库、地图与托管产品继续推迟决定。
